services:
  postgres:
    image: postgres:16-alpine
    container_name: eventos-postgres-prod
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres_secret}
      POSTGRES_DB: eventhub
    volumes:
      - postgres_prod_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: valkey/valkey:7-alpine
    container_name: eventos-redis-prod
    volumes:
      - redis_prod_data:/data

  minio:
    image: minio/minio:RELEASE.2022-10-29T06-21-33Z
    container_name: eventos-minio-prod
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY}
    volumes:
      - minio_prod_data:/data
    command: server /data --console-address ":9001"

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
      target: prod
    container_name: eventos-api-prod
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - MINIO_ENDPOINT=${MINIO_ENDPOINT}
      - MINIO_PORT=${MINIO_PORT}
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
      - MINIO_BUCKET_EVENTS=${MINIO_BUCKET_EVENTS}
      - MINIO_BUCKET_CERTIFICATES=${MINIO_BUCKET_CERTIFICATES}
      - MINIO_EXTERNAL_URL=${MINIO_EXTERNAL_URL}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_FROM=${SMTP_FROM}
      - APP_URL=${APP_URL}
      - API_URL=${API_URL}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile
      target: prod
    container_name: eventos-frontend-prod
    environment:
      - NODE_ENV=production
      - PORT=3000
      - API_URL=${API_URL}
    depends_on:
      - api

  worker:
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
      target: prod
    container_name: eventos-worker-prod
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - MINIO_ENDPOINT=${MINIO_ENDPOINT}
      - MINIO_PORT=${MINIO_PORT}
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
      - MINIO_BUCKET_EVENTS=${MINIO_BUCKET_EVENTS}
      - MINIO_BUCKET_CERTIFICATES=${MINIO_BUCKET_CERTIFICATES}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_FROM=${SMTP_FROM}
      - APP_URL=${APP_URL}
      - API_URL=${API_URL}
      - MINIO_EXTERNAL_URL=${MINIO_EXTERNAL_URL}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  nginx:
    build:
      context: ./infra/nginx
    container_name: eventos-nginx-prod
    ports:
      - "3050:80"
    depends_on:
      - api
      - frontend

volumes:
  postgres_prod_data:
  redis_prod_data:
  minio_prod_data:
